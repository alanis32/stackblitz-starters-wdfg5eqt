function dividir(x, y);
{
let dividir = x / y;
return (dividir);
}
console.log(dividir(40, 4))

function multiplicar (z, o);
{
  let multiplicar = z * o;
  return (multiplicar);
}
console.log(multiplicar(5, 5))