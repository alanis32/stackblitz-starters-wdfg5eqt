// Objetivo de uma funçã  = definir um bloco de código  reutilizavéis
// functio <nome> (parametros>){..}
function soma(x,y) // recebe dados como parâmetro 
{
  let soma = x + y; // busquem declarar apenas variaveis LET nas functions
  return (soma); // retornar a informação 
}
// chmar a função
var print;
 print1= soma(soma (4,7),3); console.log (print1);
 print2= soma(10, soma(print1,5)); console.log (print2);
 console.log(soma(print1, print2));


